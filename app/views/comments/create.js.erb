<% if comment.valid? %>
  $('<%= escape_javascript(render "comments", comments_presenter: CommentPresenter.wrap(comment.article.comments)) %>').html('.comments')
  $('#new_comment')[0].reset()
<% else %>
  $('<%= escape_javascript(render "form", comment: comment) %>').html('#new_comment')
<% end %>
